# https://docs.readthedocs.io/en/stable/config-file/v2.html
version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: latest
  commands:
    - pip install uv
    - uv run --locked zensical build --clean
    - mkdir -p $READTHEDOCS_OUTPUT; mv site $READTHEDOCS_OUTPUT/html
